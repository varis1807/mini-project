let DisplayArea = (props) => {
  return (
    <div>
      <p>Name- {props.name}</p>
      <p>Email- {props.email}</p>
      <p>Phone- {props.phone}</p>
    </div>
  );
};

export default DisplayArea;
